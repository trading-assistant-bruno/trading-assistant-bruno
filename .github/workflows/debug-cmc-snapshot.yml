name: Debug CMC snapshot
on:
  push:
    branches: [btc-turbo-point-in-time]
    paths: ['src/debug_cmc_snapshot.py','.github/workflows/debug-cmc-snapshot.yml']
  workflow_dispatch:
jobs:
  debug:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with: {python-version: '3.12'}
      - run: pip install pandas requests lxml html5lib
      - run: python src/debug_cmc_snapshot.py
