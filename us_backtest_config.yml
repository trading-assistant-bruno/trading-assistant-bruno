capital_usd: 10000
risk_per_trade_pct: 0.5
max_positions_green: 4
max_positions_orange: 1

backtest:
  start_date: "2018-01-01"
  warmup_start_date: "2016-12-01"
  end_date: null
  commission_pct_one_way: 0.05
  min_commission_usd: 1.0
  stop_limit_max_slippage_pct: 0.5
  exit_modes: [sma50, target_2r, target_3r]

universe:
  mode: sp500_point_in_time
  min_price: 5
  min_avg_dollar_volume: 5000000
  min_history_days: 260

market:
  vix_green_max: 25

strategy:
  sma50_slope_days: 20
  sma200_slope_days: 20
  max_distance_from_52w_high_pct: 25
  min_distance_above_52w_low_pct: 30
  min_perf_6m_pct: 10
  min_rs_6m_vs_spy_pct: 0
  min_rs_rank: 80
  base_lookback_days: 50
  max_base_depth_pct: 25
  min_base_quality_score: 45
  pivot_lookback_days: 60
  pivot_exclude_recent_days: 5
  entry_buffer_pct: 0.1
  max_distance_above_pivot_pct: 1.5
  ready_below_pivot_pct: 0.7
  watchlist_below_pivot_pct: 3.0
  min_stop_distance_pct: 3
  max_stop_distance_pct: 8
