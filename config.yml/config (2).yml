capital_eur: 10000
risk_per_trade_pct: 0.5
max_positions_green: 4
max_positions_orange: 1
max_new_candidates: 8
universe:
  max_symbols: 5000
  min_price: 5
  min_avg_dollar_volume: 5000000
market:
  vix_green_max: 25
fx:
  eurusd_fallback: 1.1
strategy:
  sma50_slope_days: 20
  sma200_slope_days: 20
  max_distance_from_52w_high_pct: 25
  min_distance_above_52w_low_pct: 30
  min_perf_6m_pct: 10
  min_rs_6m_vs_spy_pct: 0
  min_rs_rank: 80
  base_lookback_days: 50
  max_base_depth_pct: 25
  min_base_quality_score: 45
  pivot_lookback_days: 60
  pivot_exclude_recent_days: 5
  entry_buffer_pct: 0.1
  max_distance_above_pivot_pct: 1.5
  stop_limit_max_slippage_pct: 0.5
  ready_below_pivot_pct: 0.7
  watchlist_below_pivot_pct: 3.0
  max_extension_retain_pct: 4.0
  min_stop_distance_pct: 3
  max_stop_distance_pct: 8
validation:
  earnings_block_days: 14
  min_chart_validation_score: 75
  min_final_base_quality_score: 70
  max_data_age_hours: 18
  metadata_pause_seconds: 0.3
  earnings_fetch_retries: 3
  earnings_retry_pause_seconds: 0.8
  earnings_cache_max_age_days: 14
  manual_earnings_dates:
    CM:
      date: '2026-08-27'
      source: CIBC IR — VÉRIFIÉ
corporate_events:
  enabled: true
  news_lookback_days: 180
  manual_exclude_tickers:
    OGN:
      type: M&A / ACQUISITION
      reason: Accord définitif de rachat de Organon par Sun Pharma à 14 USD par action ; titre exclu de la stratégie swing
        momentum tant que l'opération reste en cours.
    TALK:
      type: M&A / ACQUISITION
      reason: Acquisition de Talkspace par Universal Health Services en cours, à 5,25 USD par action ; titre exclu de la stratégie
        swing momentum tant que l'opération n'est pas clôturée ou abandonnée.
